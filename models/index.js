// files to export all the models so that the directory can be referance not the files
// basically this is an index for the directory
module.exports = {
  Habit: require("./habit"),
  User: require("./Users")

}